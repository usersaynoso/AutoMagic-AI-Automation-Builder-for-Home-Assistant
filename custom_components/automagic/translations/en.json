{
  "config": {
    "step": {
      "user": {
        "title": "Connect to LLM",
        "description": "Enter the URL of your OpenAI-compatible LLM endpoint. Ollama default is http://localhost:11434 — no other Ollama config needed.",
        "data": {
          "endpoint_url": "LLM Endpoint URL"
        }
      },
      "model": {
        "title": "Model Settings",
        "description": "Select the model and configure generation parameters.",
        "data": {
          "model": "Model",
          "max_tokens": "Max Tokens",
          "temperature": "Temperature",
          "context_limit": "Entity Context Limit"
        }
      }
    },
    "error": {
      "cannot_connect": "Cannot connect to LLM endpoint. Check the URL and ensure the server is running."
    },
    "abort": {
      "already_configured": "AutoMagic is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "AutoMagic Settings",
        "data": {
          "model": "Model",
          "max_tokens": "Max Tokens",
          "temperature": "Temperature",
          "context_limit": "Entity Context Limit"
        }
      }
    }
  }
}
